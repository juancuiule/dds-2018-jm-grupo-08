package dominio.reactores;

//import org.quartz.CronScheduleBuilder;
//import org.quartz.JobBuilder;
//import org.quartz.JobDetail;
//import org.quartz.Scheduler;
//import org.quartz.SchedulerException;
//import org.quartz.Trigger;
//import org.quartz.TriggerBuilder;
//import org.quartz.impl.StdSchedulerFactory;

public class CronComunicarSensor {

//	public static void main(String[] args) throws SchedulerException{
		
		/*JobDetail job = JobBuilder.newJob(JobComunicarSensor.class).build();
		
		//Trigger trigger = TriggerBuilder.newTrigger().withIdentity("triggersimple").startNow().build();
		
		Trigger trigger = TriggerBuilder.newTrigger().withIdentity("triggerCron").withSchedule(CronScheduleBuilder.cronSchedule("5 * * ? * * *")).build();
		
		Scheduler sr = StdSchedulerFactory.getDefaultScheduler();
		
		sr.start();
		sr.scheduleJob(job, trigger);
		*/
//	}

}
